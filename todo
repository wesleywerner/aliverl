[top]

* handle player death by dropping the current state and going
    to a game over state.


* draw the full stats screen:

    + level
    + turn

    + health
    + mana
    + stealth
    + attack
    + score

* fill out the base map tile blocking tiles
* complete ai.def
* flesh out ten levels with tile and objects.
* copy blocking info to other levels

* write story to match each level.

* add a score counter to the player
